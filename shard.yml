name: smaz
version: 0.1.0

authors:
  - mentalblood0 <necheporenko.stepan@yandex.ru>

crystal: '>= 1.17.0'

scripts:
  postinstall: cd src/ && git clone --depth 1 https://github.com/antirez/smaz && cd smaz && gcc -c smaz.c -O2 -Wall -W -ansi -pedantic

license: MIT
